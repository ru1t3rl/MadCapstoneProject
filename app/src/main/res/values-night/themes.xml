<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MadCapstoneProject" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/background_night</item>
        <item name="colorPrimaryVariant">@color/background_night_dark</item>
        <item name="colorOnPrimary">@color/background_accent</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/background_night</item>
        <item name="colorSecondaryVariant">@color/background_night_dark</item>
        <item name="colorOnSecondary">@color/background_accent_dark</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>